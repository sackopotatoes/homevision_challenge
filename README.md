<!-- @format -->

# Requirements

```
Docker
or
Node 16
```

# Running

To run the dockerized version:

```
docker-compose up
```

Otherwise, ensure node 16 is installed and run:

```
npm install
npm run build
npm start
```
